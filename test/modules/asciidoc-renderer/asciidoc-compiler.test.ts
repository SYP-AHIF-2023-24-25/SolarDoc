import { assert } from "chai";
import {AsciidocFile} from "@solardoc/asciidoc-renderer";

describe("AsciidocCompiler", () => {
    describe("compile()", () => {
        it("should return presentation", async () => {
            assert.isTrue(true, "Test not implemented yet");
        });
    });
});