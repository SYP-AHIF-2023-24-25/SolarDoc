import type {EditorClient} from "@/scripts/editor/ot/editor-client";

export class ServerOTAdapter {
  /**
   * Registers the callbacks for the server adapter and allows the server to interact with the editor.
   * @param editorClient The editor client to interact with.
   * @since 1.0.0
   */
  registerCallbacks(editorClient: EditorClient) {
    // TODO!
  }

  sendSelection(selection: Selection) {
    // TODO!
  }
}
